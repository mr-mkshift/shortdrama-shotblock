---
name: shortdrama-shotblock
description: 숏드라마·시리즈물을 공장식으로 찍어내기 위한 분경조(分镜组) 컷 블록을 한국어 입력에서 중국어 또는 영어 프롬프트로 컴파일한다. 캐릭터 상수(음색·성격·참조도)와 컷 변수(상태·어조·대사)를 분리하고, 조명을 재질 하이라이트 상대비로 잠그고, 시작·종료 상태를 텍스트로 고정해 다음 화로 인계한다. 사용자가 숏드라마·연속물·에피소드 프롬프트, 분경조·컷 시트·스토리보드 블록, 캐릭터 바이블·더빙 지시·음색 고정, 컷 간 연결·다음 화 인계, 여러 편을 같은 룩으로 반복 생산하는 것을 언급하면 적극적으로 발동한다. Dreamina/Doubao/Seedance/Veo/Sora 어디로 내보내든 문법은 이 스킬이 잡는다. 단일 이미지 프롬프트나 룩북 모델 얼굴 작업은 대상이 아니다.
license: MIT
version: 1.1
---

# 숏드라마 분경조 컴파일러

## ⛔ 시작 전 — 이 순서를 건너뛰지 않는다

답을 쓰기 전에 아래를 순서대로 수행한다. 하나라도 건너뛰면 결과물이 정본과 어긋난다.

1. **사용자가 `[프로젝트 카드]` 를 줬는지 본다.** 있으면 그것이 이번 작품의 고정층이고,
   이 문서의 일반 규칙보다 **우선한다.** 특히 `이 작품의 예외` 칸.
2. **타깃 언어를 확정한다.** Dreamina·Doubao·Seedance → **중국어** / Veo·Sora·Runway → **영어**.
   사용자가 명시하지 않았는데 툴 이름만 있으면 위 대응표를 따른다. 임의로 영어를 고르지 않는다.
3. **해당 문법 파일을 실제로 연다.** 중국어면 `references/grammar-zh.md`, 영어면 `references/grammar-en.md`.
   배경이 있으면 `references/scene-space.md` 도 연다.
   **파일을 열지 않은 채 `[분경조]` 를 출력하면 그 응답은 실패다.**
   블록 명칭·순서·템플릿은 그 파일이 정본이다. 기억으로 지어내거나 **줄여 쓰지 않는다.**
4. 그다음에 컴파일한다.

이 문서에는 블록 템플릿을 싣지 않는다. 일부러 그렇게 했다 —
여기 두면 파일을 안 열고 기억으로 쓰게 되고, 파일이 갱신돼도 따라오지 못한다.

## ⛔ 출력 계약

이 스킬이 발동되면 아래 형식으로만 출력한다. 산문 해설·머리말·"어떠신가요" 금지.

```
[분경조]        타깃 언어(중국어 또는 영어) 전문. 한국어 섞지 않는다.
[대조]          같은 내용의 한국어. 검수용. "대조 빼줘" 하면 생략한다.
[인계 카드]     다음 화에 그대로 붙여넣을 종료 상태 + 첫 대사.
```

캐릭터 바이블을 처음 만들거나 고쳤을 때만 그 앞에 `[캐릭터 바이블]` 을 덧붙인다.
매번 다시 뱉지 않는다 — 이건 상수고, 상수를 반복 출력하는 순간 이 스킬의 존재 이유가 사라진다.

확인을 구하지 말고 바로 컴파일한다. 정보가 모자라면 **모자란 것만** 묻는다.

---

## ⛔ 참조 자료는 형태다, 내용이 아니다

사용자가 잘 짜인 프롬프트를 예시로 건넬 때 — 남의 팀 것이든 자기 전작이든 — 그건 **문법을 보여주려고** 준 것이다. 거기 담긴 캐릭터·의상·소도구·대사·장소·종료 상태는 그 작품의 재료지 이번 작품의 재료가 아니다.

| 가져온다 | 안 가져온다 |
|---|---|
| 블록 순서와 라벨 이름 | 캐릭터와 이름 |
| 칸의 종류 (무엇을 적는 자리인가) | 의상·소도구·헤어 |
| 값의 형식 (배수·초·각도·색온도) | 대사 한 줄 |
| 제약 조항이 놓이는 위치 | 장소와 시대 |
| 상수/변수를 가르는 기준 | 종료 상태와 인계 내용 |
| | 참조 이미지 파일명 |
| | 그 작품 특유의 이펙트 |

**참조 프롬프트의 문장이 결과물에 그대로 나타나면 실패다.** 빈칸은 사용자가 이번 작품에 대해 말한 것으로 채운다. 채울 재료가 없으면 참조에서 빌려오지 말고 **묻는다.** 참조에 있던 값을 기본값으로 슬쩍 넣는 것도 같은 실패다.

판별법 하나 — 결과물의 한 줄을 짚고 **"이 값을 사용자가 이번 대화에서 말했나"** 를 묻는다. 아니라면 참조에서 새어 나온 것이다.

이 스킬 안의 예시도 똑같이 취급한다. 예시는 모양을 보여줄 뿐이다. 예시에 나온 인물이나 소품이 사용자 결과물에 등장하면 그건 버그다.

---

## 이 스킬이 하는 일

숏드라마는 한 편의 예술품이 아니라 **80편짜리 생산 라인**이다. 그래서 목표가 다르다.

- 100%의 일관성을 시간 들여 잡는 게 아니라, **90%를 한 번의 생성으로** 잡는다
- 리롤 비용과 편집 시간을 줄이는 쪽으로 프롬프트 구조를 짠다
- 컷과 컷, 화와 화 사이의 연결을 **사람이 확인하지 않아도** 굴러가게 만든다

이 목표에서 나오는 원칙 하나: **바꿀 곳을 미리 정해둔다.**
프롬프트를 고정층과 변동층으로 쪼개면, 80편을 찍으면서 손대는 곳이 컷 표 몇 칸으로 줄어든다.

---

## 1. 고정층 / 변동층

이 분리가 스킬의 척추다. 어느 층에 속하는지 헷갈리면 **"2화 뒤에도 같은 값인가"** 로 판단한다.

### 고정층 — 프로젝트 단위. 한 번 쓰고 매 컷 재사용

| 블록 | 담는 것 | 왜 고정인가 |
|---|---|---|
| 스타일·질감 | 화면비, 렌더 스타일, 금지 목록(자막·워터마크·BGM) | 시리즈 정체성. 흔들리면 시청자가 딴 작품으로 본다 |
| 광영 설계 | 색온도, 광원 배치, 심도, **재질별 하이라이트 상대비** | 룩 일관성의 대부분이 여기서 결정된다 |
| 소재 바인딩 | `@캐릭터 = 파일명` + 그 소재가 고정하는 요소 열거 | 얼굴·의상이 화마다 바뀌면 시리즈가 아니다 |
| 공간 잠금 | 배경 구역의 좌우 순서·높이, 인물이 서는 구역, 심도 등급별 f값 | 구역이 흔들리면 컷마다 딴 장소가 된다 |
| 캐릭터 음색표 | 캐릭터별 음색·성격 | 목소리는 인물에 붙박이다 |
| 최우선 접미 | 이탈 금지, 대사 정확 일치, 자막 금지 | 매 컷 맨 뒤에 붙어야 힘이 있다 |

### 변동층 — 컷마다 채운다

| 블록 | 담는 것 |
|---|---|
| 씬 헤더 | 장소·시간·실내외, 근경/중경/원경 인물 배치 |
| 등장 목록 | 이번 컷의 인물·소도구 + **여기 없는 것 등장 금지** 조항 |
| 컷 N | 길이(초), 화각/앵글, 상태, 액션 1~2줄, 더빙 지시, 대사 |
| 취경 | 이번 컷에 들어오는 배경 구역 + 흐림 정도. 배경이 변하면 그 상태도 |
| 종료 상태 | 캐릭터별 마지막 상태 한 줄씩 |
| 인계 | 다음 화 첫 프레임 + 첫 대사 |

---

## 2. 조명은 형용사가 아니라 **비율**로 잠근다

이게 이 문법에서 가장 값나가는 부분이다.

"칼이 번쩍인다"는 생성할 때마다 달라진다. 형용사에는 기준점이 없기 때문이다.
대신 **재질 세 종의 상대비**로 쓰면 몇 번을 다시 뽑아도 관계가 유지된다.

```
금속 하이라이트 ×1.5 / 피부 ×0.5 / 천 ×0.3
```

피부와 천을 눌러서 금속만 튀게 만드는 구조다. 절대값이 아니라 **비**라서, 전체 노출이 밝게 나오든 어둡게 나오든 "금속이 제일 튄다"는 관계는 안 깨진다.

한국어 입력에 "칼이 번쩍였으면 좋겠어" 같은 말이 오면 그대로 옮기지 말고 **이 장면의 주인공 재질이 뭔지** 되물어 비율로 변환한다. 프롬프트 예산의 1/5을 여기 써도 아깝지 않다.

자세한 설계법과 재질 표는 `references/lighting-materials.md`.

---
## 3. 배경은 한 장으로 잠그지 말고 **구역으로 쪼갠다**

배경 참조도는 공간만이 아니라 **구도까지** 잠그려 든다. 그래서 근경으로 들어가면 모델이 참조도의 레이아웃을 재현하려고 화각을 뒤로 빼거나, 프레임 밖에 있어야 할 요소를 우겨넣는다. 연속성 조항에 "요소 A와 B가 연속"이라고 써두면 더 심해진다 — 클로즈업에도 A와 B를 다 넣으려 든다.

셋을 갈라놓으면 풀린다.

| 무엇이 | 무엇을 잠그나 |
|---|---|
| 배경 참조도 | 재질·색·기물. **구도는 안 잠근다** |
| 공간 잠금 블록 (고정층) | 요소의 좌우 순서와 높이 — 공간 자체 |
| 컷마다 취경 한 줄 (변동층) | 이번 컷에 어느 구역이 들어오는가 |

### 구역으로 쪼개는 법

배경을 카메라 시점 기준으로 2~4개 구역으로 나누고, 구역마다 이름과 내용을 준다. 좌우 순서와 높이는 시리즈 내내 안 바뀐다. 인물을 어느 구역 앞에 세울지도 여기서 정한다 — 이게 정해지면 단독 컷의 배경이 자동으로 결정된다.

구역이 다섯을 넘으면 관계가 흐려진다. 배경이 복잡하면 더 쪼개지 말고 뭉친다.

### 화각이 좁아질수록 배경은 색면이 된다

원경·중경에서는 배경 요소를 **이름**으로 지정한다. 근경 이하에서는 **색면과 발광면의 위치**로 지정한다. 배경이 아웃포커스로 뭉개지는 구간에서 디테일을 요구하면, 모델이 배경을 다시 그리려다 공간 구조를 바꾼다.

| 화각 | 배경을 어떻게 쓰나 |
|---|---|
| 중경 | 구역 이름 + 프레임 내 위치. 구역 경계까지 보인다 |
| 중근경 | 구역 2개 정도만. 내용은 색층으로 남고 디테일은 사라진다 |
| 근경 | 색면 1~2개의 위치와 화면 점유 비율. 식별 가능한 물체 없음 |
| 클로즈업 | 단일 색면 + 발광 번짐 한 점. 윤곽 없음 |

심도도 화각 등급마다 고정한다. 등급별 f값을 고정층에 못 박아두면 흐림 정도가 컷마다 안 흔들린다.

### 근경 이하에서는 배경 참조도를 호출하지 않는다

씬 헤더에서 배경 참조 태그를 뺀다. 참조도가 얻는 것(재질·색)보다 잃는 것(구도 재현 시도)이 크다. 그 컷의 배경은 취경 한 줄로 충분하다.

### 연속성 조항을 고쳐 쓴다

"요소 A·B·C가 연속"은 "매 컷에 A·B·C가 다 나온다"로 읽힌다. 이렇게 쓴다.

```
<요소들>의 상대 위치와 재질은 전 컷 연속.
단, 해당 컷의 취경 범위 안에서만 나타난다. 연속성이 곧 매 컷 전원 출연은 아니다.
```

### 배경이 발광할 때

LED월·모니터·창처럼 배경이 스스로 빛나면 그건 배경이 아니라 **광원**이다. 인물이 역광으로 눌리므로 카메라 측 정면 보조광을 얼굴 주광으로 따로 세운다.

배경 색이 바뀌는 컷에서는 인물 윤곽에 떨어지는 색 스필도 함께 적되, **얼굴 피부톤은 불변**으로 못 박는다. 안 그러면 배경이 붉어질 때 얼굴까지 붉어진다.

배경 발광이 팝인처럼 튀어야 하면 램프(점진 상승)로 쓰지 말고 **점등**으로 쓴다. 점등 시점은 대사의 특정 단어 프레임에 건다(5장). 오버슈트 후 안정까지 적고, 그 뒤로는 깜빡임·호흡 없이 고정이라고 못 박는다.

블록 명칭과 템플릿은 `references/scene-space.md`.

---


## 4. 더빙 = 상수(음색·성격) + 변수(상태·어조)

목소리는 인물에 붙박이고, 연기는 컷마다 바뀐다. 이 둘을 한 덩어리로 쓰면 컷마다 인물 설명을 다시 쓰게 되고, 그러다 값이 어긋난다.

```
상수  캐릭터 바이블에 1회      음색(맑고 차가운 소년음) · 성격(눌러 참고 자제, 말수 적고 낮게)
변수  컷마다 두 칸             상태(조롱 / 실어 / 웃음) · 어조(차갑게·느리게·문장 끝 올림)
```

대사는 **원작 문장을 그대로** 넣는다. 요약하거나 다듬지 않는다.
숏드라마는 대사가 흐름을 끌고 가는 장르라, 대사를 손대는 순간 리듬이 무너진다.

---

## 5. 연출은 카메라가 아니라 **발화 타임라인**에 건다

컷 레이아웃과 카메라 무빙에 프롬프트 예산을 쓰지 않는다. 대신 **대사의 특정 단어 시점**에 동작을 붙인다.

```
"'늦었어'라 말할 때 시선이 문으로 가고,
 '네 탓'이라 말할 때 손이 탁자에서 떨어지고,
 '그만하자'에서 의자를 밀며 일어선다"
```

(형태를 보여주는 예시다. 이 대사와 동작을 결과물에 옮겨 쓰지 않는다.)

이러면 카메라를 지시하지 않아도 연기와 화면이 붙는다. 저예산으로 연출감이 나오는 지점이 여기다.

화각은 한 컷당 하나면 충분하다(근경/중경/클로즈업 + 앵글). 그 이상은 리롤 확률만 올린다.

---

## 6. 시작·종료 상태는 텍스트로 고정한다

프레임 이미지를 레퍼런스로 넣지 않는다. **텍스트로만** 각 인물의 시작·종료 상태를 못 박는다.

이유는 편집 병목 제거다. 종료 상태가 고정되어 있으면
다음 컷·다음 화가 그 상태를 그대로 받아서 시작할 수 있고,
사람이 영상을 일일이 확인해서 연결을 맞출 일이 없다.

그래서 `[인계 카드]` 를 매번 낸다. 다음 화 작업을 시작할 때 이걸 그대로 붙여넣으면 체인이 이어진다.

칸의 모양은 이렇다. 값은 이번 작품 것으로 채운다.

```
[인계 카드]
종료 상태        <인물A>: <상태> / <인물B>: <상태> / <인물C>: <상태>
다음 첫 프레임   <렌즈>mm <인물> <샷>, <표정>, 시선은 <대상>
다음 첫 대사     <인물>: "<대사>"
```

---

## 7. 캐릭터시트는 **일부러 훼손**해서 쓴다

얼굴을 격자로 덮어, 정체성 단서(머리 모양·나이·골격·의상)만 남긴다.
영상 모델이 참조 이미지에서 얼굴을 픽셀 그대로 복사하는 것을 막기 위해서다.
클로즈업 크롭에도 같은 처리를 한다 — 이걸 안 하면 클로즈업만 레퍼런스를 그대로 베껴 온다.

사용자가 캐릭터시트를 만들거나 고르는 단계면 `references/character-bible.md` 를 읽고 안내한다.

---

## 컴파일 절차

1. **타깃과 언어를 확정한다.** 처음 한 번만 묻고 이후 대화에서 유지한다.
   - Dreamina(即梦)·Doubao·Seedance → **중국어**
   - Veo/Sora/Runway/서구권 → **영어**
   - 사용자가 명시하면 그대로 따른다
2. **프로젝트 카드가 있는지 확인한다.** 사용자가 주면 그것이 이번 작품의 고정층이고 이 스킬의 일반 규칙보다 우선한다. 없으면 캐릭터 바이블부터 만든다(이름·음색·성격·참조도 태그·고정 요소).
3. **고정층을 조립한다.** 이미 있으면 재사용하고 다시 묻지 않는다.
4. **컷을 나눈다.** 길이 합이 클립 총 길이와 **정확히** 맞아야 한다. 안 맞으면 맞춘다.
5. **컷마다 채운다.** 화각 1개 + 액션 1~2줄 + 더빙 지시 + 대사 원문 + 취경 한 줄 + 등장 제한 조항.
6. **종료 상태와 인계 카드를 쓴다.**
7. **검수한다.** 아래 목록을 통과 못 하면 고쳐서 낸다.

문법 전문과 블록 순서는 언어별 참조 파일에 있다. 컴파일 직전에 해당 파일을 읽는다.

- 중국어 출력 → `references/grammar-zh.md`
- 영어 출력 → `references/grammar-en.md`
- 배경 구역·취경 설계 → `references/scene-space.md`

---

## 검수 — 걸리면 그 초안을 내지 않는다

초안을 다 쓴 뒤 아래를 점검한다. **걸린 항목이 하나라도 있으면 출력하지 않고 고쳐서 낸다.**
점검 결과는 출력하지 않는다.

| 검사 | 무엇을 막는가 |
|---|---|
| 컷 길이 합 = 총 길이 | 어긋나면 모델이 임의로 늘리거나 자른다 |
| 조명이 비율로 표기됐나 | "밝게·번쩍이는" 같은 형용사는 재생성마다 달라진다 |
| 음색이 컷에 안 적혔나 | 컷에 음색이 있으면 상수가 두 곳에 생겨 결국 어긋난다 |
| 대사가 원문 그대로인가 | 요약·의역은 리듬을 죽인다 |
| 등장 제한 조항이 컷마다 붙었나 | 없으면 모델이 배경 인물과 소도구를 지어낸다 |
| 종료 상태가 인물 수만큼 있나 | 하나라도 비면 다음 화 인계가 끊긴다 |
| 취경 한 줄이 컷마다 붙었나 | 없으면 근경에서 모델이 배경 전체를 우겨넣으려 화각을 뒤로 뺀다 |
| 근경 이하 컷에서 배경 참조도를 뺐나 | 걸려 있으면 참조도의 구도를 재현하려 든다 |
| 최우선 접미가 맨 뒤인가 | 중간에 있으면 앞 지시에 먹힌다 |
| 연출 수사가 섞였나 | "cinematic·dramatically·숨막히는"은 정보가 0이다 |
| 타깃 언어 단일인가 | 한국어가 섞이면 모델이 자막으로 렌더한다 |
| 타깃 언어가 대응표와 맞나 | Seedance·Dreamina·Doubao면 중국어다. 임의로 영어를 고르지 않는다 |
| 블록 명칭·순서가 문법 파일과 일치하나 | 줄여 쓰면 모델이 다른 블록으로 읽는다 |
| 씬 헤더·등장 목록 블록이 있나 | 빠지면 모델이 배경 인물과 소도구를 지어낸다 |
| 블록 밖에 한 글자라도 썼나 | 안내 한 줄도 계약 위반이다 |

---

## 흔한 이탈 — 스스로 잡는다

첫 응답에서 아래가 자주 일어난다. 출력 전에 자기 초안이 여기 걸리는지 본다.

| 이탈 | 무엇이 잘못됐나 |
|---|---|
| 문법 파일을 안 열고 기억으로 블록을 지어냄 | 시작 전 3번을 건너뛴 것이다 |
| 블록 명칭을 줄여 씀 | `REFERENCE BINDING`→`REFERENCES`, `CORE GUIDELINES`→`CORE` 같은 축약. 정본 그대로 쓴다 |
| 씬 헤더·등장 목록 블록을 통째로 뺌 | `SCENE`·`CAST`/`PROPS` 는 변동층 필수다. 없으면 모델이 배경 인물과 소도구를 지어낸다 |
| 취경 줄을 컷에 안 붙임 | 근경에서 모델이 배경 전체를 우겨넣으려 화각을 뒤로 뺀다 |
| 등장 제한 조항을 컷마다 안 붙임 | 씬 헤더에 한 번 적는 것으로 갈음하지 않는다. 컷마다 붙어야 힘이 있다 |
| 툴 이름만 보고 임의로 영어를 고름 | Seedance·Dreamina·Doubao는 중국어다. 대응표를 따른다 |
| 블록 앞뒤에 설명 한 줄을 붙임 | "…생성합니다" 같은 안내도 계약 위반이다. 필요하면 `[대조]` 에 쓴다 |
| 캐릭터 바이블을 자유 서술로 씀 | 여덟 칸 스키마를 지킨다 — 태그·참조·외형·의상·오답·음색·성격·소도구 |
| 이 스킬을 "참고"하고 자기 형식으로 재구성 | 출력 계약은 협상 대상이 아니다 |

---

## 하지 않는 것

- **카메라 무빙 장식.** 돌리·크레인·시네마틱 팬을 지시하지 않는다. 발화 싱크로 대체한다
- **레퍼런스 프레임 이미지.** 시작·종료는 텍스트로만 고정한다
- **대사 창작.** 원작 대사를 받아 쓴다. 사용자가 새로 써달라고 명시하면 그때만 쓴다
- **타인 IP의 캐릭터명·대사 이식.** 문법은 범용이고, 내용은 사용자 것을 쓴다
- **매번 캐릭터 바이블 재출력.** 바뀔 때만 낸다
- **배경 디테일을 근경까지 끌고 가기.** 흐려질 구간은 색면으로 쓴다
- **화면 내 문자를 맨땅에서 생성시키기.** 참조도에 없는 글자를 새로 그리라고 하면 깨진다.
  다만 **참조도에 이미 박혀 있는 글자는 복제가 잘 된다** — 그때는 생성에 넣고 "참조도와 逐字 일치, 자막이 아니라 세트 안의 표시물"이라고 못 박는다.
  자막 바·로고 오버레이·프레임 위에 얹는 고지는 언제나 편집 합성이고, 대조에 한 줄로 적는다

---

## 참조 파일

| 파일 | 언제 읽나 |
|---|---|
| `references/grammar-zh.md` | 중국어로 출력할 때. 블록 명칭·순서·전문 템플릿 |
| `references/grammar-en.md` | 영어로 출력할 때 |
| `references/character-bible.md` | 캐릭터 상수표를 만들 때, 캐릭터시트 훼손을 안내할 때 |
| `references/lighting-materials.md` | 조명·재질 비율을 설계할 때. 장면 유형별 표 있음 |
| `references/scene-space.md` | 배경을 구역으로 쪼갤 때, 컷별 취경을 쓸 때. 발광 배경 처리 포함 |
| `references/project-card.md` | 프로젝트 상수를 다음 세션·다른 에이전트에 넘길 때. 첫 컴파일 전에 만든다 |
